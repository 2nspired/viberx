# ============================================
# VibeRX Environment Variables
# ============================================
# Copy this file to .env and fill in your values
# NEVER commit .env to git - it contains secrets!
#
# Environment variables are validated at build time using T3 Env.
# See src/env.ts for the validation schema.
# ============================================

# --------------------------------------------
# DATABASE (Supabase PostgreSQL)
# --------------------------------------------
# Get this from: Supabase Dashboard > Project Settings > Database > Connection string
# Use the "Transaction pooler" connection string for serverless environments
# Format: postgresql://[user]:[password]@[host]:[port]/[database]?pgbouncer=true
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres?pgbouncer=true"

# Direct connection for Prisma migrations (non-pooled) - OPTIONAL
# Get this from: Supabase Dashboard > Project Settings > Database > Connection string > URI
# Only needed if using Prisma migrations (not db push)
DIRECT_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres"

# --------------------------------------------
# SPOTIFY API CREDENTIALS
# --------------------------------------------
# Get these from: https://developer.spotify.com/dashboard
# 1. Create a new application
# 2. Note the Client ID and Client Secret
# 3. Add redirect URIs in the app settings

# Your Spotify app's Client ID (32 character string)
SPOTIFY_CLIENT_ID="your_spotify_client_id_here"

# Your Spotify app's Client Secret (keep this secret!)
SPOTIFY_CLIENT_SECRET="your_spotify_client_secret_here"

# Redirect URI for OAuth callback
# Development: http://localhost:3000/api/auth/callback
# Production: https://your-domain.vercel.app/api/auth/callback
# IMPORTANT: This must match exactly what's registered in your Spotify Developer Dashboard
SPOTIFY_REDIRECT_URI="http://localhost:3000/api/auth/callback"

# --------------------------------------------
# AUTHENTICATION & SECURITY
# --------------------------------------------
# Secret key for encrypting session data
# MUST be at least 32 characters for security
# Generate with: openssl rand -base64 32
AUTH_SECRET="your_auth_secret_here_must_be_at_least_32_characters_long"

# --------------------------------------------
# ADMIN & DEVELOPMENT (Optional)
# --------------------------------------------
# Password for accessing /admin/design-system route in development
# Only used when NODE_ENV=development
ADMIN_PASSWORD="your_admin_password_here"

# --------------------------------------------
# BUILD OPTIONS
# --------------------------------------------
# Skip environment validation during build
# Useful for Docker builds where env vars aren't available yet
# SKIP_ENV_VALIDATION="true"

# --------------------------------------------
# VERCEL (Auto-populated on Vercel deployment)
# --------------------------------------------
# These are automatically set by Vercel - don't add them manually
# VERCEL_URL - Your deployment URL
# VERCEL_ENV - 'production', 'preview', or 'development'
